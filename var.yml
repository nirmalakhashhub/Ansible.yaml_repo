#Ansible variables:

---
 - hosts: prd
   become: yes
   name: understanding Ansible Variables
   vars:
     greetings: Hello everyone!

   tasks:
    - name: Ansible Simple Variable Example Usage
      debug:
        msg: "{{ greetings }}, Let’s learn Ansible variables"

...
